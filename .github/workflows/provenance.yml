name: Publish to npm registry

on:
  workflow_dispatch:
    inputs:
      debug:
        description: 'Enable debug output'
        required: false
        default: '0'
        type: string
        options:
          - '0'
          - '1'

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    uses: SocketDev/socket-registry/.github/workflows/provenance.yml@43a668e11f52d9f6a19ced20a0b3ee6e5cdde7e5 # main
    with:
      debug: ${{ inputs.debug }}
      package-name: '@socketsecurity/sdk'
      setup-script: 'pnpm run build'
      use-trusted-publishing: true
