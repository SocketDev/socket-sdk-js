name: Claude Auto Review

on:
  pull_request:
    types: [opened]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  pull-requests: read

jobs:
  auto-review:
    uses: SocketDev/socket-registry/.github/workflows/claude-auto-review.yml@08fba31a9abec77127b476c7eebc745fccd9feef # main
    secrets:
      anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
