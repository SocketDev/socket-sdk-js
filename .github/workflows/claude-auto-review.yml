name: Claude Auto Review

on:
  pull_request:
    types: [opened]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  pull-requests: read

jobs:
  auto-review:
    uses: SocketDev/socket-registry/.github/workflows/claude-auto-review.yml@dba06046a487c9b3f32941e3ea40e1866d419ce8 # main
    secrets:
      anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
