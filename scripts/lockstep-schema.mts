/**
 * @fileoverview Re-export shim — real schema lives in lockstep/schema.mts.
 * Kept for backward compatibility with imports from `./lockstep-schema.mts`.
 */

export * from './lockstep/schema.mts'
